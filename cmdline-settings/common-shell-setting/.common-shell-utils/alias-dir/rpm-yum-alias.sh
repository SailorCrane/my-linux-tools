
# =================== rpm-key =====================
alias rpm_kl="ls /etc/pki/rpm-gpg"      # rpm key list
alias rpm_ka="rpm --import"             # rpm key add
alias rpm_kdir="ls /etc/pki/rpm-gpg/"   # rpm key dir
