
# xxd 和 hexdump一个是大端, 一个是小端模式(都是两个字节一组显示)
alias hex="xxd"        # 按照实际存储字节显示(小端), 而且可以尽量显示出byte对应的ascii
#alias hex="hexdump"     # 大端模式显示

# hexedit工具
# hexedit   (命令行) : 可以直接编辑空白文件, <Ctrl-x> 保存退出
# ghex,
# bless,
