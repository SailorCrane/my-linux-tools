# vim: set fdm=marker  fdl=1: vim modeline( set )


# alias
# {{{
alias sz="source ~/.zshrc"
alias mzb="man zshbuiltins"
alias mza="man zshall"
# }}}

# suffix -s alias
# {{{
alias -s txt=vim  # vim to edit  plain text
alias -s c=vim  # vim to edit  plain text
alias -s cpp=vim  # vim to edit  plain text
alias -s vim=vim  # vim to edit  vim script
alias -s vimperatorrc=vim  # vim to edit ~/.vimperatorrc

alias -s zip=unzip
alias -s gz=gunzip          # gzip 的解压缩命令: gunzip
alias -s tar="tar xvf"      # tar 的解压缩命令: tar xvf
# }}}
