1: grep set awk 共同选项

-f  指定脚本文件
-e  后面指定命令

2: grep 独有选项
    -l 只输出匹配文件名
    -n  输出匹配行同时, 也输出行号. 类似于cat -n
    -o  --only 只输出匹配部分(part), 可以用这个选项验证自己比较长的正则.
    -P  perl 风格正则表达式.

3: sed 独有选项
    -n 只输出匹配行, 而不是输出所有行.

4: awk 独有选项
    -F 设置分割符, 默认是空格. 所以如果分隔符选择空格, 就不要用-F选项了.
